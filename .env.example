DATABASE_URL=postgresql://user:password@localhost:5432/failfast
POSTGRES_USER=failfast
POSTGRES_PASSWORD=changeme
POSTGRES_DB=failfast
# Comma-separated origins for browser CORS (learner web + marketing site).
# Include every browser origin that calls the API (comma-separated, no spaces).
CORS_ORIGIN=https://learner.failfastng.com,https://learner.failfastedu.com,https://failfast-learner-web.pages.dev,https://www.failfastng.com,https://www.failfastedu.com
NODE_ENV=production
